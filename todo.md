# ICE Excel - TODO

## P0 核心缺失（没有就不像 Excel）

- [x] 撤销/重做完整覆盖 — 格式化操作（背景色、字体色、边框、对齐、字号等）目前无法撤销，只有文本编辑支持 undo/redo
- [x] 列宽自适应 — 双击列边界自动调整到内容最大宽度
- [x] 行高自适应完善 — 换行文本、富文本场景下行高自动计算
- [x] 拖拽填充智能序列 — 日期序列、星期序列、自定义序列（一月~十二月等）
- [x] 状态栏统计 — 选中区域时底部显示 SUM / AVERAGE / COUNT / MIN / MAX

## P1 公式能力补齐

- [x] 高频函数补齐：SUMIF, SUMIFS, COUNTIF, COUNTIFS, AVERAGEIF
- [x] IFERROR, IFNA, IFS
- [x] INDEX + MATCH 组合
- [x] CONCATENATE / TEXTJOIN
- [x] LEFT, RIGHT, MID, FIND, SUBSTITUTE（确认是否已有）
- [x] ROUND 系列：ROUNDUP, ROUNDDOWN, INT, TRUNC
- [x] 跨 Sheet 引用验证 — `Sheet1!A1` 和 `Sheet1!A1:B10` 语法完整支持
- [x] 公式自动补全 — 输入 `=SU` 时弹出候选列表

## P2 体验完善

- [x] 工具栏状态同步 — 选中加粗单元格时加粗按钮高亮，选中有背景色的单元格时颜色选择器同步
- [x] 右键菜单补全 — 插入批注、设置单元格格式对话框、选择性粘贴入口
- [x] 单元格格式对话框 — 数字格式、对齐、字体、边框、填充的统一设置面板
- [x] 冻结窗格 UI — 当前有冻结功能但需要确认 UI 入口是否直观
- [x] 名称框交互 — 点击名称框输入 `A1:B10` 跳转并选中区域
- [x] 打印预览优化 — 分页预览、页眉页脚设置
- [x] 键盘快捷键补全 — Ctrl+B/I/U 加粗/斜体/下划线、Ctrl+1 打开格式对话框、Ctrl+; 插入当前日期

## P3 数据能力

- [x] XLSX 导入兼容性 — 合并单元格、条件格式、数字格式的导入保真度
- [x] XLSX 导出兼容性 — 导出后用 Excel 打开格式不丢失
- [x] 大文件导入 — 10MB+ XLSX 文件的流式解析，避免卡死浏览器
- [x] CSV 导入编码检测 — 自动识别 UTF-8 / GBK / Shift-JIS
- [x] 数据透视表 — `src/pivot-table` 目录已存在，确认完成度并补全

## P4 协同编辑健壮性

- [ ] OT 边界测试 — 合并单元格 + 行列插删 + 公式的并发冲突场景
- [ ] 离线编辑 + 重连同步 — 断网后本地编辑，重连后自动合并
- [ ] 协同光标优化 — 远程用户选区的半透明背景色显示
- [ ] 操作历史冲突 — 多人同时撤销/重做的一致性

## P5 性能持续优化

- [ ] 样式密集渲染 — 当前 450 个全样式单元格 ~20ms，大面积财务报表可能触顶
- [ ] 公式 Worker 批量调度优化 — 100 个公式 Worker 计算 ~106ms，可优化消息传递开销
- [ ] 内存占用 — 10万行空单元格的内存开销，考虑稀疏存储
- [ ] Canvas 离屏渲染 — 冻结窗格区域可用 OffscreenCanvas 缓存
